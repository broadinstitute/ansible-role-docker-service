---
docker_service_app_directory: /app
docker_service_user: jenkins
docker_service_health_check: 'curl -k -f https://localhost/status'
docker_service_health_timeout: "60"

pip_version: "9.0.*"
pip_get_pip_version: "latest"
pip_executable: "pip"